{
  "name": "linkcom",
  "version": "1.0.4",
  "description": "LinkCOM - Web 串口共享与远程调试工具 (WebSocket 转发)",
  "main": "server.js",
  "scripts": {
    "version:sync": "python sync_version.py",
    "start": "python sync_version.py && node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
